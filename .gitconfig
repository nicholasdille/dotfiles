[push]
	default = simple
[credential]
	helper = store
[user]
	name = Nicholas Dille
[includeif "gitdir:**/.cfg"]
    path = .gitconfig-private
[includeif "gitdir:**/haufe/**"]
    path = .gitconfig-haufe
[includeif "gitdir:**/private/**"]
    path = .gitconfig-private
[includeif "gitdir:**/johnic/**"]
    path = .gitconfig-johnic
